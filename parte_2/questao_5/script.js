const melhorGuitarrista = "Jimmy hendrix";

let a= 1, b = 2, c = 3

/*
A diferença entre as formas de declaração let e const, é que a variavel const é uma CONSTANTE, ou seja, não pode ser alterada
Já a variavel let pode ser alterada quantas vezes for necessario durante o programa

Declarar uma variavel utilizando var e até mesmo não utilizar declaração é uma má pratica pois dessa forma, as variaveis serão lidas
fora dos escopos planejados pra elas, sendo lidas fora do escopo de grupo e dentro do escopo global respectivamente.
*/